spring:
  application:
    name: api-gateway
  config:
    import: "configserver:" # Dòng này phải thụt vào 2 dấu cách
  cloud:
    config:
      uri: http://localhost:9296 # Dòng này cũng thụt vào 2 dấu cách