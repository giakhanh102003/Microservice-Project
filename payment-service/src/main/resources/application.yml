# D:\Microservice-Project\payment-service\application.yml
#server:
#  port: 0   # để random port, vì Eureka sẽ quản lý service

spring:
  application:
    name: payment-service
  config:
    import: "configserver:http://localhost:8888"

